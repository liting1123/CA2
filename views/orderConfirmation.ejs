<!DOCTYPE html>
<html>
<head>
    <title>Order Confirmation</title>
    </head>
<body>
    <div class="container">
        <h2 class="mt-4">Order Confirmation</h2>

        <% if (order) { %>
            <p>Thank you for your order! Your order #<%= order.idorder %> has been placed.</p>
            <h3>Order Details:</h3>
            <p><strong>Order Date:</strong> <%= new Date(order.orderDate).toLocaleString() %></p>
            <p><strong>Total Price:</strong> $<%= order.totalPrice.toFixed(2) %></p>
            <p><strong>Status:</strong> <%= order.status %></p>

            <h4>Items:</h4>
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Product</th>
                        <th>Image</th>
                        <th>Quantity</th>
                        <th>Price</th>
                        <th>Subtotal</th>
                    </tr>
                </thead>
                <tbody>
                    <% order.items.forEach(item => { %>
                        <tr>
                            <td><%= item.name %></td>
                            <td><img src="/images/<%= item.image %>" width="50" class="img-thumbnail"></td>
                            <td><%= item.quantity %></td>
                            <td>$<%= item.price.toFixed(2) %></td>
                            <td>$<%=(item.quantity * item.price).toFixed(2)%></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>

            <div class="mt-4">
                <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
                <a href="/checkout/<%= order.idorder %>" class="btn btn-success">Proceed to Checkout</a>
            </div>

        <% } else { %>
            <p>No order details found.</p>
            <a href="/dashboard" class="btn btn-primary">Go to Dashboard</a>
        <% } %>
    </div>

    </body>
</html>