<!DOCTYPE html>
<html>
<head>
    <title>Dashboard</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
      <div class="container-fluid">
        <a class="navbar-brand" href="/dashboard">Foodiess</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if (user && user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/inventory">Inventory</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/addInventory">Add</a>
            </li>
            <% } %> 
            <li class="nav-item">
              <a class="nav-link" href="/logout">Logout</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Food</h2></div>
        <br>
        <div class="text-center">
          <% if (menuItems) { %> 
            <form action="/editInventory/<%= menuItems.idmenuItems %>" method="POST" enctype="multipart/form-data">
              <label for="name">Food Name:</label><br>
                <input type="text" id="name" name="name" value="<%= menuItems.name %>" required><br><br>

                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value="<%= menuItems.image %>" readonly><br>
                <img src = "/images/<%= menuItems.image %>" width="20%"><br><br>

                New Image:<br>
                <input type="file" id="image" name="image" accept="images/*" value="<%= menuItems.image %>"><br><br>


                <label for="quantity">Food Quantity:</label><br>
                <input type="number" id="quantity" name="quantity" value="<%= menuItems.quantity %>" required><br><br>

                <label for="price">Food Price:</label><br>
                <input type="number" id="price" name="price" min="0" step="0.01" value="<%= menuItems.price %>" required><br><br>

                <label for="category">Food Category:</label><br>
                <input type="text" id="category" name="category" value="<%= menuItems.category %>" required><br><br>

                <button type="submit">Update Food</button>
            </form>
          <% } else { %>
              <p>No Food found.</p>
          <% } %>
          <a href ="/Inventory">Back</a>
        </div>
    </div>
</body>
</html>
